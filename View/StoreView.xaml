<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="StoreApp.View.StoreView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#B9E4C9">

    <Grid>
        <Grid Background="White" Margin="40" CornerRadius="25">
            <GridView SelectionMode="None" Margin="0 45" HorizontalAlignment="Center" ScrollViewer.HorizontalScrollMode="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Visible">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="250" Margin="5">
                            <StackPanel Orientation="Vertical">
                                <Image Height="250" Width="200"/>
                                <TextBlock Text="name" Style="{StaticResource productName}"/>
                                <TextBlock Text="price" Style="{StaticResource productPrice}"/>
                                <Button Content="Добавить в корзину" Style="{StaticResource addProduct}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
                <GridView.Items>
                    <TextBlock/>
                    <TextBlock/>
                    <TextBlock/>
                    <TextBlock/>
                    <TextBlock/>
                </GridView.Items>
            </GridView>
        </Grid>
    </Grid>
</Page>
